import aa from 'search-insights';

import '../styles/global.css';

export default function App({ Component, pageProps }) {
    aa('init', {
        appId: "P7A16UOSI5",
        apiKey: "fa824448a8e0177a3980d48da0415703"
    }); 

    return <Component {...pageProps} />
}
